SIMPLE=123xyz
# NOTE that .env specific interpolation only occurs at a local level.
# We have another file that re-declares SIMPLE, but this local one is the interpolated one.
INTERPOLATED="Multiple\nLines and variable substitution: ${SIMPLE}"
